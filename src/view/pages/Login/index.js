import React from "react";
import { LoginContainer } from "../../containers";

const Login = () => {
  return (
    <div className="row">
      <LoginContainer />
    </div>
  );
};

export default Login;
